# ZLM CI 비활성화

2026-09-12 사용자 요청으로 upstream GitHub Actions 13개를 이 디렉터리로 이동했다.
푸시·PR·이슈에 따른 자동 빌드, 테스트, 아티팩트 업로드 및 Docker 게시를 실행하지 않는다.
기존 정의는 검토용으로 보존하며, 재활성화는 별도 승인 후 수행한다.

이 변경은 CI에만 적용한다. MediaServer의 영상 중계·녹화와 로컬 빌드는 변경하지 않는다.
EdgeFlowAI 서버 저장소의 GitLab 미디어 빌드 잡도 별도로 비활성화한다.
